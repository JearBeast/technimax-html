$(function () {
    var e = null,
        t = null;
    $(".header__nav3>li,.header__nav1>li,.header__nav5>li").hover(
        function () {
            clearTimeout(t),
                $(".header__nav3__submenu, .header__nav5__submenu, .header__nav1__submenu, .header__nav3__dropcart").removeClass("d-lg-block"),
                (e = $(this).find(".header__nav3__submenu, .header__nav5__submenu, .header__nav1__submenu, .header__nav3__dropcart")).addClass("d-lg-block");
        },
        function () {
            $(this).parent().hasClass("header__nav5")
                ? e.removeClass("d-lg-block")
                : (t = setTimeout(function () {
                      e.removeClass("d-lg-block");
                  }, 800));
        }
    ),
        $(".advantages")
            .not(".advantages--4")
            .slick({ infinite: !0, slidesToShow: 3, slidesToScroll: 1, responsive: [{ breakpoint: 768, settings: { slidesToShow: 3, slidesToScroll: 1 } }] }),
        $(".advantages--4").slick({
            infinite: !0,
            slidesToShow: 4,
            slidesToScroll: 1,
            adaptiveHeight: !0,
            responsive: [
                { breakpoint: 992, settings: { slidesToShow: 3, slidesToScroll: 1 } },
                { breakpoint: 768, settings: { slidesToShow: 1, slidesToScroll: 1 } },
            ],
        }),
        $(".products__slick")
            .not(".products__slick--4")
            .slick({ infinite: !0, slidesToShow: 3, slidesToScroll: 3, adaptiveHeight: !0, infinite: false, dots: true, responsive: [{ breakpoint: 768, settings: { slidesToShow: 2, slidesToScroll: 1 } }] });
    $(".products__slick--4").slick({
        infinite: !0,
        slidesToShow: 4,
        slidesToScroll: 4,
        adaptiveHeight: !0,
        responsive: [
            { breakpoint: 992, settings: { slidesToShow: 3, slidesToScroll: 1 } },
            { breakpoint: 768, settings: { slidesToShow: 2, slidesToScroll: 1 } },
            { breakpoint: 450, settings: { slidesToShow: 1, slidesToScroll: 1 } },
        ],
    });
    $(".rubrics__slick").slick({
        infinite: !0,
        slidesToShow: 2,
        slidesToScroll: 1,
        adaptiveHeight: !0,
        dots: true,
        infinite: false,
        mobileFirst: true,
        responsive: [
        	{ breakpoint: 1200, settings: "unslick" },
            { breakpoint: 992, settings: { slidesToShow: 3, slidesToScroll: 1 } },
            { breakpoint: 768, settings: { slidesToShow: 2, slidesToScroll: 1 } }
        ],
    });
    $(".hp-main__slick").slick({
        infinite: !0,
        slidesToShow: 1,
        slidesToScroll: 1,
        adaptiveHeight: !0,
        arrows: false,
        dots: true
    });
    $(document).on("click", ".button__dropdown__opener, .button--settings", function () {
        return $(".button__dropdown__open, .button__dropdown__open2").addClass("d-none"), clearTimeout(null), $(this).parents(".button__dropdown").find(".button__dropdown__open").toggleClass("d-none"), !1;
    }),
    $(document).on("mouseover", ".button__dropdown__opener2", function () {
        $(this).parent().find(".button__dropdown__open2").removeClass("d-none");
    }),
    $(window).on("click", function () {
        $(".button__dropdown__open, .button__dropdown__open2").addClass("d-none");
    }),
    $(".tabs__mobilheader").click(function () {
        $(this).parent().find(".tabs__header").slideToggle();
    }),
    $(".tabsSlick__mobilheader").click(function () {
        $(this).parent().find(".tabsSlick__slick").slideToggle();
    }),
    $(window)
        .resize(function () {
            767 < $(window).width() ? $(".tabs__header").not(".tabs__header--nocollapse").show() : $(".tabs__header").not(".tabs__header--nocollapse").hide();
        })
        .resize(),
    $(".detail__cart__minus").click(function () {
        var e = parseInt($(this).parent().find("input").val()) - 1;
        0 == e && (e = 1), $(this).parent().find("input").val(e), $(this).parent().find("input").change();
    }),
    $(".detail__cart__plus").click(function () {
        var e = parseInt($(this).parent().find("input").val()) + 1,
            t = $(this).parent().find("input").attr("max");
        void 0 !== t && !1 !== t && t < e && (e = t), $(this).parent().find("input").val(e), $(this).parent().find("input").change();
    }),
    $(".detail__mainGallery").slick({ infinite: !0, slidesToShow: 1, slidesToScroll: 1 }),
    $(".magnific:not(.slick-cloned)").magnificPopup({ type: "image", gallery: { enabled: !0 } }),
    $(".magnific-side").magnificPopup({ type: "image", gallery: { enabled: !0 } }),
    $(".magnific-video").magnificPopup({ type: "iframe", gallery: { enabled: !0 } }),
    $(".tabs").each(function () {
        var e = $(this),
            t = $(this).find(".tabs__header .tabs__header__item--active").index();
        -1 == t && (t = 1),
            $(this).find(".tabs__header .tabs__header__item").removeClass("tabs__header__item--active"),
            $(this).find(".tabs__body .tabs__body__item").hide(),
            $(this).find(".tabs__header .tabs__header__item").eq(t).addClass("tabs__header__item--active"),
            $(this).find(".tabs__body .tabs__body__item").eq(t).show(),
            $(".products__slick--4").slick("setPosition", 0),
            $(this)
                .find(".tabs__header .tabs__header__item")
                .click(function () {
                    (t = $(this).index()),
                        e.find(".tabs__header .tabs__header__item").removeClass("tabs__header__item--active"),
                        e.find(".tabs__body .tabs__body__item").hide(),
                        e.find(".tabs__header .tabs__header__item").eq(t).addClass("tabs__header__item--active"),
                        e.find(".tabs__body .tabs__body__item").eq(t).show(),
                        $(".products__slick--4").slick("setPosition", 0);
                });
    }),
    $(".modal__tabs").each(function () {
        var e = $(this),
            t = $(this).find(".modal__tabs__header a.active").index();
        -1 == t && (t = 1),
            $(this).find(".modal__tabs__header a").removeClass("active"),
            $(this).find(".modal__tabs__body .modal__tabs__item").hide(),
            $(this).find(".modal__tabs__header a").eq(t).addClass("active"),
            $(this).find(".modal__tabs__body .modal__tabs__item").eq(t).show(),
            $(this)
                .find(".modal__tabs__header a")
                .click(function () {
                    (t = $(this).index()),
                        e.find(".modal__tabs__header a").removeClass("active"),
                        e.find(".modal__tabs__body .modal__tabs__item").hide(),
                        e.find(".modal__tabs__header a").eq(t).addClass("active"),
                        e.find(".modal__tabs__body .modal__tabs__item").eq(t).show();
                });
    }),
    $('[data-toggle="tooltip"]').tooltip(),
    $(".parameters").each(function () {
        $(this).find(".parameters__title").hasClass("parameters__title--open") ? $(this).find(".parameters__hidden").show() : $(this).find(".parameters__hidden").hide(),
            $(this)
                .find(".parameters__title")
                .click(function () {
                    $(this).hasClass("parameters__title--open")
                        ? ($(this).parent().find(".parameters__hidden").slideUp(), $(this).removeClass("parameters__title--open"))
                        : ($(this).parent().find(".parameters__hidden").slideDown(), $(this).addClass("parameters__title--open"));
                });
    }),
    $.isFunction($.fn.selectric) && $(".selectric").selectric(),
    $(".jsButtonConfig").click(function () {
        $(this).parent().find(".config").removeClass("d-none"), $(this).hide();
    }),
    $(".jsButtonConfigClose").click(function () {
        $(this).parent().parent().find(".jsButtonConfig").show(), $(this).parent().addClass("d-none");
    }),
    $(window)
        .resize(function () {
            $(".tabsSlick__slick").slick({
                slidesToShow: 4,
                slidesToScroll: 1,
                variableWidth: !0,
                prevArrow: '<span class="icon-arrow-right slick-prev"></span>',
                nextArrow: '<span class="icon-arrow-right slick-next"></span>',
                responsive: [{ breakpoint: 768, settings: "unslick" }],
            });
        })
        .resize(),
    $(".slickVideo").slick({
        slidesToShow: 4,
        slidesToScroll: 1,
        responsive: [
            { breakpoint: 1024, settings: { slidesToShow: 3 } },
            { breakpoint: 768, settings: { slidesToShow: 2 } },
        ],
    }),
    $(".modal__mainGallery").slick({ slidesToShow: 1, slidesToScroll: 1 }),
    $(".info-pop").on("click", function () {
        $("#infomodal .modal-title").html($(this).attr("data-title")), $("#infomodal .modal-body").html($(this).attr("data-body")), $("#infomodal").modal("show");
    }),
    $("tr.collapsed").click(function () {
        var e = $(this).data("for");
        $('tr[data-open="' + e + '"]').toggleClass("d-none"), $(this).toggleClass("collapsed--open");
    }),
    $(".collapsed").each(function () {
        if ($(this).hasClass("collapsed--open")) {
            e = $(this).data("for");
            $('tr[data-open="' + e + '"]').removeClass("d-none");
        } else {
            var e = $(this).data("for");
            $('tr[data-open="' + e + '"]').addClass("d-none");
        }
    }),
    $(document).on("change", ".form__file", function (e) {
        var t = $(this.nextElementSibling),
            i = this.files && 1 < this.files.length ? (this.getAttribute("data-multiple-caption") || "").replace("{count}", this.files.length) : e.target.value.split("\\").pop();
        if (i) t.text(i);
        else {
            var s = $(this).data("caption");
            s && s.length ? t.text(s) : t.text("-");
        }
    }),
    $(".products__slick").hover(function () {
        $(".products__slick").css("z-index", 1), $(this).css("z-index", 1e3);
    }),
    $(".header__mobilsearch").click(function () {
        return $(".header__search").toggleClass("d-none"), $("#searchInput").focus(), !1;
    }),
    $(".header__menu").click(function () {
        $(".header__nav5").toggleClass("d-none").toggleClass("d-flex");
        $('.header__nav5, .header__menu, .header .bg-lightgray').toggleClass('open');
        $('.header__usernav').addClass('d-none');
    }),
    $(".header__menu2").click(function () {
        $(".header__menu2List").toggle();
    }),
    $(".jsButtonVice").click(function () {
        $(".text--max").toggleClass("text--max--open"), $(".text--max").hasClass("text--max--open") ? $(this).html($(this).attr("data-less")) : $(this).html($(this).attr("data-more"));
    });
});

$(document).on("click", ".header__nav5 > li .next_level", function () {
    $(this).toggleClass('open');
    $(this).parent('li').children('.header__nav5__submenu').toggleClass('d-none');
});

$(document).on("click", ".header__search__input", function () {
    $('.header #search-url').addClass('open');
});
$(document).click(function (e){
    var container = $(".header #search-url");
    if (!container.is(e.target) && container.has(e.target).length === 0) {
	    $('.header #search-url').removeClass('open');
	}
});

$(document).on("click", ".header .header_second_menu .icon", function () {
    $('.header__usernav').toggleClass('d-none');
    $('.header__menu').removeClass('open');
    $('.header__nav5').addClass('d-none');
    $('.header__nav5').removeClass('d-flex');
});
$(document).on("click", ".header__usernav__bottom > li .next_level", function () {
    $(this).toggleClass('open');
    $(this).parent('li').children('ul').toggleClass('d-none');
});

$(document).ready(function() {
	var sHeight = $('.products__slick .slick-track').height() + 8;
	console.log(sHeight);
	$('.products__slick .slick-track').css('height', sHeight + 'px');
});  



"use strict";

$(function () {

	window.is = window.is || {};


	is.object = function (value) {
		return value && typeof value === 'object' && value.constructor === Object;
	};

});
;

"use strict";

$(function () {

	window.LiveFormValidationHelper = window.LiveFormValidationHelper || {};


	/**
	 * @param {HTMLElement} form
	 * @return {boolean}
	 */
	LiveFormValidationHelper.isValid = function (form) {

		return window.Nette && Nette.validateForm && Nette.validateForm(form);

	};

});
;

"use strict";

$(function () {

	$.nette.init();


	LiveForm.setOptions({
		showMessageClassOnParent: false,
		controlErrorClass: 'form__input--error',
		controlValidClass: 'form__input--valid',
		messageErrorClass: 'form__error text-right',
		messageTag: 'div',
		messageErrorPrefix: ''
	});


	$(document).on('click', '#eu-cookies__button', function () {
		let date = new Date();
		date.setFullYear(date.getFullYear() + 10);
		document.cookie = 'eu-cookies=1; path=/; expires=' + date.toUTCString();
		$('#eu-cookies').hide();
	});


	$(document).on('click', 'button.notification__close-button', function () {
		$(this).parents('.notification').remove();
	});


	$(document).on('click', '.buy .buyAjax', function () {
		var buyId = $(this).attr('data-buy');
		var buyCount = $(this).attr('data-count');
		var buyKonfigurace = $(this).attr('data-konf');
		var buyKonfiguraceAmount = $(this).attr('data-konf-amount');

		if (buyKonfigurace !== undefined) {
			var confBuyKonfigurace = buyKonfigurace.split("-");
			var confBuyKonfiguraceAmount = [];
			if (buyKonfiguraceAmount !== undefined) {
				confBuyKonfiguraceAmount = buyKonfiguraceAmount.split("-");
			}
			//$("html, body").animate({scrollTop: 0}, "slow");
			$.nette.ajax({
				'url': '?do=addToBasketKonfigurace',
				'data': {
					id: buyId,
					count: buyCount,
					konf: confBuyKonfigurace,
					konfamounts: confBuyKonfiguraceAmount
				}
			}).done(function () {
				/*$("#basketContent").removeClass('d-none');
				setTimeout(function () {
					$("#basketContent").addClass('d-none');
				}, 3000);*/
			});
		} else {
			//$("html, body").animate({scrollTop: 0}, "slow");
			$.nette.ajax({
				'url': '?do=addToBasket',
				'data': {
					id: buyId,
					count: buyCount
				}
			}).done(function () {
				/*$("#basketContent").removeClass('d-none');
				setTimeout(function () {
					$("#basketContent").addClass('d-none');
				}, 3000);*/
			});
		}
	});


	$(document).on('click', '.compare-button', function (e) {
		e.preventDefault();

		let
			$this = $(this),
			data = {'id': $this.data('rel')};

		$("html, body").animate({ scrollTop: 0 }, "slow");

		if ($this.hasClass('active')) {
			$.nette.ajax({
				url: '?do=porovnatRemove',
				data: data
			});

			$this.removeClass('active');
			$this.text($this.data('add'));

			return;
		}

		$.nette.ajax({
			url: '?do=porovnat',
			data: data
		});

		$this.addClass('active');
		$this.text($this.data('remove'));
	});


	$(document).on('click', '.favourite-button', function (e) {
		e.preventDefault();
		$("html, body").animate({scrollTop: 0}, "slow");
		var rel = $(this).attr("data-rel");
		if ($(this).hasClass('active')) {
			$(this).removeClass("active");
			$(this).text($(this).attr('data-add'));
			$.nette.ajax({
				'url': '?do=favouriteRemove',
				'data':{
					id: rel
				}
			}).done(function () {
				$("#listsContent").removeClass('d-none');
				setTimeout(function () {
					$("#listsContent").addClass('d-none');
				}, 2000);
			});
		} else {
			$(this).addClass("active");
			$(this).text($(this).attr('data-remove'));
			$.nette.ajax({
				'url': '?do=favourite',
				'data':{
					id: rel
				}
			}).done(function () {
				$("#listsContent").removeClass('d-none');
				setTimeout(function () {
					$("#listsContent").addClass('d-none');
				}, 2000);
			});
		}
	});


	$(document).on('click', '.reservationAjax', function (e) {
		e.preventDefault();
		$("html, body").animate({scrollTop: 0}, "slow");
		var buyId = $(this).attr('data-buy');
		var buyCount = $(this).attr('data-count');
		var buyKonfigurace = $(this).attr('data-konf');
		var buyKonfiguraceAmount = $(this).attr('data-konf-amount');

		if (buyKonfigurace !== undefined) {
			var confBuyKonfigurace = buyKonfigurace.split("-");
			var confBuyKonfiguraceAmount = [];
			if (buyKonfiguraceAmount !== undefined) {
				confBuyKonfiguraceAmount = buyKonfiguraceAmount.split("-");
			}
			$.nette.ajax({
				'url': '?do=addToReservationKonfigurace',
				'data': {
					id: buyId,
					count: buyCount,
					konf: confBuyKonfigurace,
					konfamounts: confBuyKonfiguraceAmount
				}
			}).done(function() {
				$("#listsContent").removeClass('d-none');
				setTimeout(function () {
					$("#listsContent").addClass('d-none');
				}, 2000);
			});
		} else {
			$.nette.ajax({
				'url': '?do=addToReservation',
				'data': {
					id: buyId,
					count: buyCount
				}
			}).done(function() {
				$("#listsContent").removeClass('d-none');
				setTimeout(function () {
					$("#listsContent").addClass('d-none');
				}, 2000);
			});
		}
	});


	$(document).on('click', '.offerAjax', function (e) {
		e.preventDefault();
		$("html, body").animate({scrollTop: 0}, "slow");
		var buyId = $(this).attr('data-buy');
		var buyCount = $(this).attr('data-count');
		var buyKonfigurace = $(this).attr('data-konf');
		var buyKonfiguraceAmount = $(this).attr('data-konf-amount');

		if (buyKonfigurace !== undefined) {
			var confBuyKonfigurace = buyKonfigurace.split("-");
			var confBuyKonfiguraceAmount = [];
			if (buyKonfiguraceAmount !== undefined) {
				confBuyKonfiguraceAmount = buyKonfiguraceAmount.split("-");
			}
			$.nette.ajax({
				'url': '?do=addToOfferKonfigurace',
				'data': {
					id: buyId,
					count: buyCount,
					konf: confBuyKonfigurace,
					konfamounts: confBuyKonfiguraceAmount
				}
			}).done(function() {
				$("#listsContent").removeClass('d-none');
				setTimeout(function () {
					$("#listsContent").addClass('d-none');
				}, 2000);
			});
		} else {
			$.nette.ajax({
				'url': '?do=addToOffer',
				'data': {
					id: buyId,
					count: buyCount
				}
			}).done(function() {
				$("#listsContent").removeClass('d-none');
				setTimeout(function () {
					$("#listsContent").addClass('d-none');
				}, 2000);
			});
		}
	});


	$(document).on('click', '.offerExistsAjax', function(e) {
		e.preventDefault();
		var buyId = $(this).attr('data-buy');
		var buyCount = $(this).attr('data-count');
		var buyKonfigurace = $(this).attr('data-konf');
		var buyKonfiguraceAmount = $(this).attr('data-konf-amount');
		var offerId = $(this).attr('data-offer-id');

		if (buyKonfigurace !== undefined) {
			var confBuyKonfigurace = buyKonfigurace.split("-");
			var confBuyKonfiguraceAmount = [];
			if (buyKonfiguraceAmount !== undefined) {
				confBuyKonfiguraceAmount = buyKonfiguraceAmount.split("-");
			}
			$.nette.ajax({
				'url': '?do=addToOfferExistsKonfigurace',
				'data': {
					offerid: offerId,
					productid: buyId,
					count: buyCount,
					konf: confBuyKonfigurace,
					konfamounts: confBuyKonfiguraceAmount
				}
			})
		} else {
			$.nette.ajax({
				'url': '?do=addToOfferExists',
				'data': {
					offerid: offerId,
					productid: buyId,
					count: buyCount
				}
			})
		}
	});


	$(document).on('click', '.next-button', function () {
		$('.stepContinueInput').click();
	});


	$('#frm-searchForm').on('submit', function (e) {
		let
			$input = $('#searchInput'),
			value = $input.val();

		if (value.length) {
			return;
		}

		e.preventDefault();
		$input.focus();
	});

	let request = null;
	$('#frm-searchForm input.ajax').on({
		click: function() {
			let value = $(this).val();
			dataLayer.push({'event': 'trackWhisper', 'status': 'klik', 'whisperText': value, 'whisperClickGroup': 'Input'});
			if (value.length >= 3){
				dataLayer.push({'event': 'trackWhisper', 'status': 'klik 3+', 'whisperText': value, 'whisperClickGroup': 'Input'});
				$.nette.ajax({
					'url':'?do=search',
					'data':{
						textsSearch:value
					}
				}).done(function() {
					$("#snippet--searching").addClass('d-block');
				});

			} else {
				$("#snippet--searching").removeClass('d-block');
			}
		}, keyup: function(e) {
			let value = $(this).val();
			if (e.keyCode == 13) {
				dataLayer.push({'event': 'trackWhisper', 'status': 'Submit (ENTER)', 'whisperText': value, 'whisperClickGroup': 'Input'});
				if (request != null){
					request.abort();
					request = null;
				}
			} else {
				dataLayer.push({'event': 'trackWhisper', 'status': 'writing', 'whisperText': value, 'whisperClickGroup': 'Input'});
				if (value.length>=2){
					if (request != null){
						request.abort();
						request = null;
					}
					request = $.nette.ajax({
						'url':'?do=search',
						'data':{
							textsSearch:value
						}
					}).done(function() {
						$("#snippet--searching").addClass('d-block');
					});
				} else {
					$("#snippet--searching").removeClass('d-block');
				}
			}
		}
	});


	/**
	 * Disables an <input> HTML element with 'data-loading-text' attribute
	 * and applies the text contained in this attribute
	 *
	 * @todo Disable all submit buttons of the form
	 * @todo Make it work with inputs, buttons, anchors
	 */
	$.nette.ext('buttons', {
		before: function (jqXHR, settings) {
			if (!this.isWatched(settings)) {
				return;
			}

			this.defaultValue = settings.nette.el.val();

			settings.nette.el.prop('disabled', true);
			settings.nette.el.val(settings.nette.el.data('loading-text'));
		},
		complete: function (jqXHR, status, settings) {
			if (!this.isWatched(settings)) {
				return;
			}

			settings.nette.el.prop('disabled', false);
			settings.nette.el.val(this.defaultValue);
		}
	}, {
		isWatched: function (settings) {
			return settings.nette
				&& settings.nette.el.length
				&& settings.nette.el.is('input[type="submit"]')
				&& settings.nette.el.data('loading-text');
		},
		defaultValue: null
	});

});


$(function () {

	// Extension to disable form submit buttons if form is submitted, is valid
	// and submit buttons have 'data-loading-disable' attribute

	// Generally applies to non-ajax form submits

	let forms = document.getElementsByTagName('form');

	for (let form of forms) {
		form.addEventListener('submit', onFormSubmitted, false);
	}


	function onFormSubmitted(e) {
		let form = e.target;

		if ( ! LiveFormValidationHelper.isValid(form)) {
			return;
		}

		let submitButtons = form.querySelectorAll('input[type="submit"][data-loading-disable="true"]');

		for (let submitButton of submitButtons) {
			$(submitButton).prop('disabled', true);
			$(submitButton).val($(submitButton).attr('data-loading-text'));
		}
	}

});
